{
  "name": "kernel-auth",
  "version": "1.0.0",
  "private": true,
  "description": "KERNEL Auth — Social OAuth + License keys for KERNEL Loader",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo No build step",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.0"
  }
}
